meta {
  name: Rename / Move File
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/files/{{fileId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
}

body:json {
  {
    "name": "archive/typescript-rules"
  }
}
