meta {
  name: Read File (by ID)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/files/{{fileId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
}
