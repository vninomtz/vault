meta {
  name: Read File (Markdown)
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/files/{{fileId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: text/markdown
}
